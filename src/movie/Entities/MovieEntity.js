import {Entity} from '@s-ui/domain'

export default class MovieEntity extends Entity {
  /**
   * Movie entity will have the following properties:
   * @param {string} id
   * @param {string} title
   * @param {string} poster
   * @param {string} overview
   */
}
